import ArgumentParser
import BCFoundation

struct AddAssertionCommand: ParsableCommand {
    static var configuration = CommandConfiguration(commandName: "add-assertion", abstract: "Add an assertion to the given envelope")
    
    func run() throws {
        
    }
}
