import ArgumentParser
import BCFoundation

struct Add: ParsableCommand {
    static var configuration = CommandConfiguration(abstract: "Add an assertion to the given envelope")
    
    func run() throws {
        
    }
}
